# Python
__pycache__/
*.pyc
venv310/
venv*/

# macOS
.DS_Store

# Claude Code local settings
.claude/

# Loose desktop screenshots (not part of the documented repo layout)
Screenshot *.png

# Per-run scratch state that changes on every simulation run.
# NOTE: the trained q_table.json, eval_results_*.json and
# learning_curve_*.png ARE committed deliberately -- they're the
# measured portfolio artifacts referenced by the README.
learned_obstacles.json
