# Python
__pycache__/
*.pyc
venv310/
venv*/

# macOS
.DS_Store

# Claude Code local settings
.claude/

# Loose desktop screenshots (not part of the documented repo layout)
Screenshot *.png

# Per-run state that changes on every simulation run. The Q-table is
# untracked so routine runs don't produce commit noise -- each clone
# trains its own from scratch, which is also the honest demo of the
# learning curve.
# NOTE: eval_results_*.json and learning_curve_*.png ARE committed
# deliberately -- they're the measured portfolio artifacts the README
# refers to, and they include the final trained table for reference.
q_table.json
learned_obstacles.json
